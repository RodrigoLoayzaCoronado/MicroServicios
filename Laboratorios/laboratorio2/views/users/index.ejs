<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
</head>
<body>
    <div class="container">
        <h1><%= title %></h1>
        <a href="/" class="btn-back">← Volver al Inicio</a>
        
        <% if (error) { %>
            <div class="alert error"><%= error %></div>
        <% } %>

        <a href="/users/add" class="btn-add">+ Nuevo Usuario</a>

        <table>
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Email</th>
                    <th>Fecha Registro</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                <% if (users.length > 0) { %>
                    <% users.forEach(user => { %>
                        <tr>
                            <td><%= user.id %></td>
                            <td><%= user.name %></td>
                            <td><%= user.email %></td>
                            <td><%= new Date(user.created_at).toLocaleString() %></td>
                            <td>
                                <form action="/users/delete/<%= user.id %>" method="POST" class="delete-form">
                                    <button type="submit" class="btn-delete">Eliminar</button>
                                </form>
                            </td>
                        </tr>
                    <% }); %>
                <% } else { %>
                    <tr>
                        <td colspan="5" class="no-data">No hay usuarios registrados</td>
                    </tr>
                <% } %>
            </tbody>
        </table>
    </div>
</body>
</html>